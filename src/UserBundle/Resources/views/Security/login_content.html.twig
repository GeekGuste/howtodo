{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
<!--<div class="login-img3-body">
    <div class="container">

        <form class="login-form"  action="{{ path("fos_user_security_check") }}" method="post">
            <div class="login-wrap">
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}

                <p class="login-img"><i class="icon_lock_alt"></i></p>

                <div class="input-group">
                    <span class="input-group-addon"><i class="icon_profile"></i></span>
                    <input type="text" class="form-control" value="{{ last_username }}" placeholder="{{ 'security.login.username'|trans }}" name="_username" autofocus required="required" autocomplete="username" >
                </div>

                <div class="input-group">
                    <span class="input-group-addon"><i class="icon_key_alt"></i></span>
                    <input type="password" class="form-control" placeholder="{{ 'security.login.password'|trans }}"  name="_password" required="required" autocomplete="current-password">
                </div>

                <label class="checkbox">
                    <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                    <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                    <span class="pull-right"> <a href="#"> Forgot Password?</a></span>
                </label>


                <button class="btn btn-primary btn-lg btn-block" value="{{ 'security.login.submit'|trans }}" type="submit" id="_submit" name="_submit">Login</button>
                
            </div>
        </form>
        <div class="text-right">

        </div>
    </div>
</div>-->

  <!--<h3>SOFT INNOVATION GROUP</h3>-->
    

        <form class="login-form"  action="{{ path("fos_user_security_check") }}" method="post">
          {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}

        <div class="form-group">
          <label for="inputUsernameEmail">Utilisateur </label>
            <input type="text" class="xform-control" value="{{ last_username }}" placeholder="{{ 'security.login.username'|trans }}" name="_username" autofocus required="required" autocomplete="username" id="inputUsernameEmail" >
        </div>
        <div class="form-group">
          <label for="inputPassword">Password</label>
         <input type="password" class="xform-control" placeholder="{{ 'security.login.password'|trans }}"  name="_password" required="required" autocomplete="current-password" id="inputPassword">
                  <a class="pull-right" href="#">Mot de passe oublié?</a>

        </div>
        <div class="checkbox pull-right">
          <label>
            <input type="checkbox">
            Remember me </label>
        </div>
       <button type="submit" class="btn-bt btn-xxl green" value="{{ 'security.login.submit'|trans }}"  id="_submit" name="_submit">
        Se connecter
        </button>
      </form>
        <div class="login-or">
        <hr class="hr-or">
        <span class="span-or">or</span>
      </div>
        <div class="row">
        <div class="col-xs-6 col-sm-6 col-md-6">
        
          <a href="#" class="btn btn-block btn-social btn-facebook"><span><i class="fa fa-facebook"></i></span>&nbsp;&nbsp; Facebook</a>
        </div>
        <div class="col-xs-6 col-sm-6 col-md-6">

          <a href="#" class="btn btn-block btn-social btn-google"><span><i class="fa fa-google"></i></span>&nbsp;&nbsp;Google</a>
        </div>
      </div>
    